# Agent Rules

## Git Operations

- **NEVER commit changes without explicitly asking the user first.**
  - Always ask for confirmation before creating any commit.
  - Wait for user approval before running `git commit` commands.
